func minmax (arr) {
   mi = arr[0]; ma = min;
   for i=1, i < arr.length; i++ {
      v = arr[i];
      if v > ma {
         ma = v
      } else if v < mi {
         mi = v
      }
   }
   ret mi, ma
};
x = array(3);
x[0] = 1; x[1] = 2; x[3] = 3;
min, max = minmax(x);
out(min, max)
