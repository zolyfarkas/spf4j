CREATE TABLE SEMAPHORES (
  NAME varchar(255) NOT NULL,
  AVAILABLE_RESERVATIONS bigint(20) NOT NULL,
  MAX_RESERVATIONS bigint(20) NOT NULL,
  LAST_UPDATED_BY varchar(255) NOT NULL,
  PRIMARY KEY (NAME),
  UNIQUE KEY SEMAPHORE_NAME_PK (NAME)
);
